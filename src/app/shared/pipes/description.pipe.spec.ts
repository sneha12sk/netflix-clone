import { DescriptionPipePipe } from './description-pipe.pipe';

describe('DescriptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
